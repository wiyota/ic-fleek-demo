{
  "name": "ic-fleek-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "dfx deploy && npm --prefix ./frontend run dev",
    "build": "npm --prefix ./frontend run build",
    "start": "npm --prefix ./frontend run start",
    "lint": "npm --prefix ./frontend run lint"
  }
}
